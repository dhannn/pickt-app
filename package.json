{
    "scripts": {
        "install": "npm install --prefix ./pickt-client/ --legacy-peer-deps",
        "start": "npm-run-all -p  start-server start-client",
        "start-server": "npm start --prefix ./pickt-server",
        "start-client": "npm start --prefix ./pickt-client/"
    },
    "devDependencies": {
        "npm-run-all": "^4.1.5"
    }
}
